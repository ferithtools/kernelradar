# Rust
/target/
**/*.rs.bk
*.pdb
# Cargo.lock — committed (binary crate; pin reproducible builds + needed
# by `rustsec/audit-check` in CI).

# BPF build artifacts
crates/kernelradar-bpf/.output/
**/*.o
**/*.skel.h

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs and test output
*.log
tests/output/

# Secrets — never commit these
*.pem
*.key
config.local.toml

# Release artifacts — checksums are tracked under release-checksums/,
# the binary tarball itself is not in git (lives on GitHub Releases).
/dist/
ADMIN_README.md  # internal admin doc; never push

# Internal trackers — never commit
k-radar_backlog.md
*.local.md
